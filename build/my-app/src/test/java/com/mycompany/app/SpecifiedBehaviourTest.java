/*package com.mycompany.app.model;

import com.mycompany.app.model.SpecifiedBehaviourTest;
import junit.framework.TestCase;


public class SpecifiedBehaviourTest extends TestCase {
    public SpecifiedBehaviourTest(String name) {
        super(name);
    }

    public void testSpecifiedBehaviourT() throws Exception {
        AdventureBehaviour Temp = new SpecifiedBehaviour(10, 2, true);
        assertEquals(10, Temp.getBP());
        assertEquals(2, Temp.getBids());
        assertEquals(true, Temp.isFreeBid());
    }

    public void testSpecifiedBehaviourF() throws Exception {
        AdventureBehaviour Temp = new SpecifiedBehaviour(30, 0);
        assertEquals(30, Temp.getBP());
        assertEquals(0, Temp.getBids());
        assertEquals(false, Temp.isFreeBid());

    }
}*/